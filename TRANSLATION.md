# 中英文内容翻译说明

本文记录 2026-09-19 中英文内容迁移所使用的工具、模型、运行方式与维护约定。当前英文内容是由中文内容机器翻译生成的初稿，并非逐篇重新调查、重新编写。

## 内容结构

- 中文源内容：`docs/zh/`
- 英文内容：`docs/en/`
- 中文构建配置：`zensical.toml`
- 英文构建配置：`zensical.en.toml`
- 中英文文件一致性检查：`scripts/check_i18n.py`
- 双语站点构建：`scripts/build_site.sh`

两个内容目录必须使用相同的相对路径。例如：

```text
docs/zh/A/Angela_White.md
docs/en/A/Angela_White.md
```

中文版继续发布在 `/corn/`，以保持原有公开 URL 不变；英文版发布在 `/corn/en/`。

## 翻译工具与模型

| 项目 | 本次使用值 |
|---|---|
| 翻译引擎 | Argos Translate 1.11.0 |
| 语言方向 | Chinese (`zh`) → English (`en`) |
| 模型包 | `translate-zh_en` 1.9 |
| 模型来源 | OPUS-MT |
| 推理后端 | CTranslate2 4.8.2 |
| 分词 | SentencePiece 0.2.2 |
| 分句 | MiniSBD 0.9.5 |
| 模型许可证 | CC BY 4.0 |
| 模型包大小 | 约 83 MB |

模型包内的说明将原始模型归于 Jörg Tiedemann 与 Santhosh Thottingal 的 OPUS-MT 工作。已安装模型的元数据示例：

```json
{
  "package_version": "1.9",
  "argos_version": "1.9.0",
  "from_code": "zh",
  "from_name": "Chinese",
  "to_code": "en",
  "to_name": "English"
}
```

Argos Translate 运行库版本与模型元数据中的 `argos_version` 不同：本次使用 1.11.0 运行库加载兼容的 1.9 模型包。

## 推理配置

```text
compute_type = int8
beam_size = 1
batch_size = 32
device = cpu
inter_threads = 1
intra_threads = 1
workers = 4
```

运行环境为 4 核 ARM Neoverse-N1（`aarch64`），未使用 GPU。翻译任务按目录拆分为 4 个并行进程；每个进程使用单线程推理，实测总 CPU 占用约为 220%–270%。

## Markdown 处理

翻译前先解析 Markdown 行，避免把格式标记直接交给模型。下列内容保持原样：

- YAML front matter 及标签；
- 标题层级、列表符号和表格分隔符；
- 粗体、行内代码等 Markdown 语法；
- 链接地址、文件名和目录结构；
- 不含中文的文本片段。

仅将可翻译的标题、字段名称、链接文字和正文送入模型，再按原结构组装。迁移过程中还修复了 12 个原先没有结束分隔符的 front matter 区块。

用于批量迁移的临时翻译脚本未提交到仓库。仓库保留的是翻译结果、双语构建配置和文件一致性检查工具。

## 本次迁移规模与速度

| 指标 | 数值 |
|---|---:|
| 每种语言的文件数 | 100 |
| 中文内容字符数 | 约 154,111 |
| 汉字数量 | 约 28,676 |
| 中文内容大小 | 约 222 KB |
| 英文结果单词数 | 约 31,982 |
| 英文内容大小 | 约 227 KB |
| 优化后完整翻译耗时 | 约 5 分钟 |
| 整体有效吞吐 | 约 90–100 汉字/秒 |
| 页面处理速度 | 约 20 页/分钟 |

以上速度是本仓库在指定机器上的整体迁移速度，包含 Markdown 解析、无需翻译的标签和链接，不应视为通用的纯文本模型基准。

初始实现采用逐行翻译和默认 `beam_size=4`，单个较长条目可能需要 1–2 分钟。改为批量推理、`int8`、`beam_size=1` 和 4 个并行进程后，完成了上述最终迁移。

## 质量说明

英文版是机器翻译初稿，可能存在以下问题：

- 人名、作品名、论文名或机构名翻译错误；
- 性别代词、时态和单复数不自然；
- 成人行业术语不准确或表达生硬；
- 中文原文中的歧义被错误解释；
- 引文标题与来源页面的正式英文标题不一致。

机器翻译没有替代事实核验。公开来源链接、出生资料、作品数量和奖项等事实仍以中文条目的原始来源为基础。

## 后续维护流程

新增或修改内容时：

1. 先更新 `docs/zh/` 中的中文条目。
2. 同步更新 `docs/en/` 下相同路径的英文条目。
3. 英文内容应人工检查专有名词、代词、日期、数字和来源标题。
4. 同步两个语言目录中的字母索引与 `_meta` 文件。
5. 运行完整校验：

   ```bash
   ./scripts/build_site.sh
   ```

该命令会先确认两个内容树的文件完全对应，再以 strict 模式分别构建中文站点与英文站点。

### 英文人工校订清单

- 保留人物艺名和品牌的官方拼写；
- 对照来源恢复作品、论文和奖项的正式英文名称；
- 检查 `she/her` 等代词是否符合人物公开身份；
- 避免使用带有歧义或贬义的直译词；
- 不新增中文原文和公开来源未支持的事实；
- 保留 Markdown 链接目标及相对路径；
- 校订后再次运行双语严格构建。
