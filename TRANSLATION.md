# 中英文内容翻译说明

本文分为两部分：2026-09-19 中英文迁移的历史记录，以及后续维护和复核规则。英文内容目前是由中文内容机器翻译生成的初稿，不是逐篇重新调查或重新编写的事实核查版本。

> **英文版状态：** `docs/en/` 即使通过完整构建检查，仍应视为 draft。Zensical strict 构建只验证站点生成；前置校验器检查结构、安全和双语一致性，但两者都不验证英文表达、专有名词、数字、URL、handle、来源、奖项或任何外部事实。

## 本次维护记录（2026-09-24）

本轮维护对项目审查中可自动确认的问题做了整体修复：

- 对照中文源修复了英文正文中 `万/亿` 数量、社交账号、作品/品牌/机构名称、字段格式、语义反转、遗漏限定和坏链接；英文首页与站点描述现在明确标注机器翻译草稿仍在复核；
- 将 Elle Lee 移入正确的 `E/` 目录，为既有 `/L/Elle_Lee/` 路径增加双语重定向；补齐两种语言各 77 项的 `_meta/list.md`，删除空的 award 占位页，并修复重复标签和多余 frontmatter 分隔线；
- 将标签中的泛化 IAFD 首页和已拒 Wikipedia Draft 替换为具体人物页或删除，修复 Lia Lin 的坏链接；移除无可靠来源的怀孕传闻、现住址、净资产和关系/宗教细节等不必要敏感信息；
- 删除未转义美元、失效数字引用和不合法的原始 HTML；共享的双语 404 模板不再作为普通导航页发布，页眉语言选择器和 `hreflang` 保留当前双语路径；
- 扩展 `scripts/check_i18n.py`，在构建前检查配置契约、索引/元数据、路径、frontmatter、tags、URL/handle、`万/亿` 数量级、危险协议、原始 HTML、未定义数字引用、坏 Markdown 链接、HTTP 外链和被拒 Wikipedia Draft；
- 使用 `pyproject.toml` 与 `uv.lock` 锁定 Zensical 及传递依赖；构建改为 staging 后替换，失败不会破坏上一份 `site/`；CI 增加 PR 只读构建、分离 build/deploy 权限、限制部署到 `main` 并固定 Action SHA；
- 发布产物现在包含 `LICENSE` 和 `NOTICE`，主题改用系统字体；维护 skill 改为动态定位仓库并同步当前校验流程。

本轮没有逐条完成外部事实核验。部分来源冲突（例如出生日期、活跃期、统计口径和历史获奖信息）仍保留为待复核项；校验和 strict 构建通过不代表这些事实已被独立证实。下面的模型和速度数字是历史迁移记录，不是本次重新测量或事实核验的结果。

## 内容结构与站点依赖

- 中文源内容：`docs/zh/`
- 英文内容：`docs/en/`
- 中文构建配置：`zensical.toml`
- 英文构建配置：`zensical.en.toml`
- 锁定项目环境：`pyproject.toml`、`uv.lock`
- 中英文文档校验：`scripts/check_i18n.py`
- 双语站点构建：`scripts/build_site.sh`
- 发布归属：`LICENSE`、`NOTICE`
- 共享模板：`overrides/404.html`、`overrides/main.html`、`overrides/partials/alternate.html`

两个内容目录必须使用相同的相对路径。例如：

```text
docs/zh/A/Angela_White.md
docs/en/A/Angela_White.md
```

相应的字母索引也必须成对存在：

```text
docs/zh/A/index.md
docs/en/A/index.md
```

中文版继续发布在 `/corn/`，以保持原有公开 URL 不变；英文版发布在 `/corn/en/`。`index.md` 是内容源文件，不是根目录 `README.md` 的别名；新增或删除条目时要在两种语言中同步维护它及其中的链接和统计。

### 站点维护的本地依赖

站点维护与 Argos 翻译环境是两套用途不同的依赖：

- `pyproject.toml` 要求 Python 3.12（`>=3.12,<3.13`）和 `uv >=0.12.5,<0.13`；本地/CI 应使用同一锁定环境。Zensical 0.0.62 本身最低支持 Python 3.10，但项目锁定的是 Python 3.12。
- 直接依赖为 `pyyaml==6.0.2` 和 `zensical==0.0.62`，开发依赖为 `ruff==0.16.8`；`uv.lock` 锁定传递依赖、Python 版本范围和包哈希。首次安装或刷新环境使用 `uv sync --locked`。
- `scripts/build_site.sh` 和预览命令使用 `uv run --locked --project ...`，不在全局环境中解析未锁定的 Zensical。

### 双语文档校验的范围

`check_i18n.py` 从脚本自身位置推导仓库根目录，并且不访问网络。当前校验范围包括双语文件集合/规范路径、Zensical 配置契约、front matter 与安全 tags、人物页 H1、`list.yaml`/`list.md`、字母 `index.md` 的链接集合、本地 Markdown 目标、危险 HTML/协议、HTTP 外链、失效数字引用和未转义的 Markdown 数学符号。它还会比较双语社交 URL/handle，以及能够识别的身高、体重、作品数量、百分比和 `万/亿` 数量级；IAFD 首页和 Wikipedia Draft 不能作为条目级证据。

这些是结构、安全和双语一致性检查，不是事实核验：检查通过不能证明外部 URL 可访问、来源支持正文、作品数量正确、翻译自然或奖项信息未过时。校验器的具体规则以仓库中的脚本版本为准。

## 翻译工具与模型（历史记录）

以下工具和参数记录的是 2026-09-19 的迁移运行，不代表当前机器上仍安装了完全相同的环境：

| 项目 | 本次使用值 |
|---|---|
| 翻译引擎 | Argos Translate 1.11.0 |
| 语言方向 | Chinese (`zh`) → English (`en`) |
| 模型包 | `translate-zh_en` 1.9 |
| 模型来源 | OPUS-MT |
| 推理后端 | CTranslate2 4.8.2 |
| 分词 | SentencePiece 0.2.2 |
| 分句 | MiniSBD 0.9.5 |
| 模型许可证 | CC BY 4.0 |
| 模型包大小 | 约 83 MB |

模型包内的说明将原始模型归于 Jörg Tiedemann 与 Santhosh Thottingal 的 OPUS-MT 工作。已安装模型的元数据示例：

```json
{
  "package_version": "1.9",
  "argos_version": "1.9.0",
  "from_code": "zh",
  "from_name": "Chinese",
  "to_code": "en",
  "to_name": "English"
}
```

Argos Translate 运行库版本与模型元数据中的 `argos_version` 不同：历史迁移使用 1.11.0 运行库加载兼容的 1.9 模型包。

## 推理配置（历史记录）

```text
compute_type = int8
beam_size = 1
batch_size = 32
device = cpu
inter_threads = 1
intra_threads = 1
workers = 4
```

历史运行环境为 4 核 ARM Neoverse-N1（`aarch64`），未使用 GPU。翻译任务按目录拆分为 4 个并行进程；每个进程使用单线程推理，当时记录的总体 CPU 占用约为 220%–270%。这些是历史测量值，不是质量保证，也不应视为通用基准。

## Markdown 处理与受保护字符串方案

### 历史迁移中已记录的规则

翻译前先解析 Markdown，避免把格式标记直接交给模型。迁移记录中曾保持以下内容原样：

- YAML front matter 及标签；
- 标题层级、列表符号和表格分隔符；
- 粗体、行内代码等 Markdown 语法；
- 链接地址、文件名和目录结构；
- 不含中文的文本片段。

仅将可翻译的标题、字段名称、链接文字和正文送入模型，再按原结构组装。迁移过程中还记录过修复 12 个原先没有结束分隔符的 front matter 区块。用于批量迁移的临时翻译脚本没有提交到仓库。

这些历史描述不等于对当前每一对文件完成了逐 token 验证。尤其是数字、日期、URL、handle 和 ID，仍必须按下述方案检查。`check_i18n.py` 的静态检查可以在翻译结果上发现部分路径、链接、handle 和数值不一致，但它不是占位符恢复器，也不能替代事实复核。

### 后续自动化的最低保护方案

后续批量翻译应把受保护字符串当作不可翻译的 token，而不是让模型“顺手修正”：

1. **先解析并枚举。** 从 Markdown 语法树或等效的行/span 解析中提取链接目标 URL、裸 URL、`@handle`、数字、日期、ID、代码 span，以及 front matter 和标签值；先提取 URL/handle，再处理普通文本中的数字，避免把 URL 内部字符拆开。
2. **替换为稳定的占位符。** 为每个出现位置分配唯一、不会出现在原文中的占位符，例如 `__CORN_PROTECT_0001__`；只把占位符周围的自然语言送给模型。链接文字可以翻译，但链接目标必须保持不变；正文中的 handle 也必须保持原样。
3. **原样恢复。** 将占位符恢复为源文件中的精确字符串，不根据模型输出“修正”大小写、标点、查询参数、路径或 ID。可选的 `/gender=f` 等后缀只有在实际复制的来源链接中出现过时才保留。
4. **做后置断言。** 翻译前后比较受保护 token 的 multiset、出现次数和顺序；URL、handle、数字/日期/ID 任一缺失、增加或改变时使任务失败并转人工检查。结构检查还应确认中英文文件路径相同、Markdown 表格和链接目标完整。
5. **保留审计信息。** 对每个文件记录源文件、翻译工具/模型版本、占位符映射和失败项；自动检查只能证明字符串没有被改写，不能证明它们对应的事实正确。

本节记录的是翻译批处理应采用的占位符保护方案和验收标准；即使仓库已有 `check_i18n.py` 的静态校验，也不能据此声称现有英文已经通过占位符恢复检查或事实核验。

## 质量说明与仍未完成的外部事实复核

英文版是机器翻译初稿，可能存在以下问题：

- 人名、作品名、论文名、机构名或品牌名翻译错误；
- 性别代词、时态和单复数不自然；
- 成人行业术语不准确或表达生硬；
- 中文原文中的歧义被错误解释；
- 引文标题与来源页面的正式英文标题不一致；
- 数字、日期、作品数量、奖项结果或来源范围被错误翻译；
- URL、账号 handle、相对路径或 Markdown 结构被改写。

机器翻译没有替代事实核验。至少以下内容仍需由人工回到公开来源逐条复核：

- 法定姓名/艺名、国籍、出生日期和地点等身份信息；
- 性别认同、性取向、健康、家庭关系和其他敏感信息；
- 活跃年代、作品数量、平台账号及其统计口径；
- 奖项、提名、年份、类别、获奖结果和正式英文名称；
- Wikipedia/Wikidata 等二手资料与 IAFD、官方或行业来源之间的差异；
- 每个外部 URL、账号 handle、来源标题和引文是否仍准确、可访问且确实支持该句。

本次文档维护没有完成上述任何外部事实核验。遇到来源缺失、冲突或无法确认的内容，应明确标记未公开/未确认或暂不写入，而不是用翻译结果填空。

## 人工复核流程

新增或修改内容时，按以下顺序处理：

1. **冻结中文源。** 记录中文文件路径、源提交或版本以及本次要改动的字段；确认英文文件位于相同相对路径。
2. **先做结构检查。** 运行双语文档校验，确认 front matter、标题、表格、链接目标、元数据和目录 `index.md` 仍成对存在，并先修复校验器报告的错误。
3. **运行受保护字符串检查。** 对数字、日期、URL、handle、ID 和代码做逐项差异检查；失败时先修复或回到源文件，不要手工猜测 token。
4. **做英文语言复核。** 人工检查专有名词和官方拼写、标题和奖项正式名称、术语、语法、时态、代词、语气及敏感身份表述；不能仅按中文性别或标签推断英文代词。
5. **做外部事实复核。** 逐项打开公开来源，确认来源确实支持事实，并记录来源名称、URL 和核验日期；对动态数据注明统计口径，对冲突保留说明。中文与英文不一致时不能以任一机器译文单独证明事实，必须回到来源。此步骤是人工工作，不能由 strict 构建代替。
6. **同步双语维护文件。** 更新两种语言的条目、字母 `index.md`、相关 `_meta` 数据；不要创建内容目录 README，也不要把旧的 award 占位文件当作生成结果。
7. **严格构建并检查差异。** 运行下节的完整命令，阅读 `git diff`，确认只包含源文件；构建成功后仍保留“英文草稿/待事实复核”状态，除非确实完成了上述复核。

### 英文人工校订清单

- 保留人物艺名、品牌、作品和来源的官方拼写；
- 对照来源恢复作品、论文、机构和奖项的正式英文名称；
- 检查 `she/her` 等代词是否符合人物公开且有来源支持的自我表述；
- 避免带有歧义、贬义或不符合行业语境的直译词；
- 不新增中文原文和公开来源未支持的事实；
- 保留 Markdown 链接目标、相对路径、URL 和 handle；
- 明确区分“获奖”“提名”“数据库记录”和“未确认”；
- 校订后再次运行双语 strict 构建。

## 严格构建与完整校验命令

从仓库根目录运行：

```bash
uv run --locked --project . python scripts/check_i18n.py --root .
uv run --locked --project . zensical build --config-file zensical.toml --clean --strict
uv run --locked --project . zensical build --config-file zensical.en.toml --clean --strict
```

等价的仓库封装命令是：

```bash
./scripts/build_site.sh
```

封装脚本会从自身位置推导仓库根目录，先在 `uv run --locked` 环境中执行完整的双语文档校验，再在临时 staging 目录中以锁定的 Zensical 0.0.62 分别执行中文和英文 `--clean --strict` 构建。它会检查两个入口、双语 404 和旧路径重定向，复制 `LICENSE`/`NOTICE`，最后才替换正式 `site/`；中途失败会保留上一份产物。校验器或构建报告的 Markdown、链接、元数据和安全问题应先修复；构建成功仍不表示外部来源、事实或翻译已经复核。`site/` 构建产物不应提交。

固定版本的本地预览命令为：

```bash
uv run --locked --project . zensical serve --config-file zensical.toml
uv run --locked --project . zensical serve --config-file zensical.en.toml
```

## 本次迁移规模与速度（历史记录）

| 指标 | 数值 |
|---|---:|
| 每种语言的文件数 | 100 |
| 中文内容字符数 | 约 154,111 |
| 汉字数量 | 约 28,676 |
| 中文内容大小 | 约 222 KB |
| 英文结果单词数 | 约 31,982 |
| 英文内容大小 | 约 227 KB |
| 优化后完整翻译耗时 | 约 5 分钟 |
| 整体有效吞吐 | 约 90–100 汉字/秒 |
| 页面处理速度 | 约 20 页/分钟 |

以上是指定机器上的整体迁移速度，包含 Markdown 解析、无需翻译的标签和链接，不应视为通用的纯文本模型基准。初始实现采用逐行翻译和默认 `beam_size=4`，单个较长条目可能需要 1–2 分钟；改为批量推理、`int8`、`beam_size=1` 和 4 个并行进程后，完成了当时记录的最终迁移。上述速度和时间不表示本次重新运行，也不表示英文内容已经完成事实核验。
