[project]
name = "corn-site"
version = "0.0.0"
description = "Reproducible bilingual Zensical site build"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pyyaml==6.0.2",
    "zensical==0.0.62",
]

[dependency-groups]
dev = [
    "ruff==0.16.8",
]

[tool.uv]
required-version = ">=0.12.5,<0.13"
